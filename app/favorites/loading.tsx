import CircularProgress from "@mui/material/CircularProgress";
import React from "react";

const Loading = () => {
  return <CircularProgress color="secondary" className="py-20" />;
};

export default Loading;
